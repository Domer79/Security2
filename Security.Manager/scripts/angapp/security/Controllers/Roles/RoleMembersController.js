function RoleMembersController($scope) {
    $scope.selectMember = function(member) {
    }
}

RoleMembersController.$inject = ['$scope'];

itisExports.controllers.RoleMembersController = RoleMembersController;